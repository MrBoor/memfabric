[project]
name = "memfabric"
version = "0.1.0"
description = "Self-organizing memory layer for LLMs"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.6.0",
    "uvicorn>=0.30.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
]
